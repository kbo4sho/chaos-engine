{
  "name": "chaos-engine",
  "version": "1.0.0",
  "description": "Matter.js 2D Physics Sandbox",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.2.0"
  }
}
